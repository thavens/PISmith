# Agents

## Python Environment

- Use `uv` for all Python-related tasks (installing packages, running scripts, managing dependencies).
- Use the local `.venv` virtual environment in this repository.
